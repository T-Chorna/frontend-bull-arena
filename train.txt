 
 Перероби наступний метод:
 componentDidUpdate(prevProps: OldMatadorProps) {
    const { applause } = this.props;
    if (prevProps.applause !== applause) {
      // console.log(applause);
      playApplauseSound(applause);

      if (applause === 3 && this.previousApplause !== 3) {
        console.log(applause);
        this.setState((prevState) => ({
          forceRender: !prevState.forceRender,
        }));
      }

      // Оновлюємо попереднє значення applause
      this.previousApplause = applause;
    }

}
Я заборонила ререндр компонента при зміні в пропсах applause, але я хочу знати, коли приходить нове значення, щоб запускати playApplauseSound(applause);  
і у випадку надходження оплесків з номером 3 необхідно змінювати стан. Тому даний код не підходить, можливо требба використати  static getDerivedStateFromProps, або щось інше замість  componentDidUpdate 